name: 部署文档

on:
  push:
    branches: [ main ]
    paths:
      - 'packages/docs/**'
      - '.github/workflows/docs.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: 设置pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: 设置Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: 安装依赖
        run: pnpm install

#      - name: 构建核心包
#        run: pnpm --filter @huoshan-ui/core build
#
#      - name: 构建 Vue 包
#        run: pnpm --filter @huoshan-ui/vue build

      - name: 建立文档
        working-directory: ./packages/docs
        run: pnpm build

      - name: 部署到GitHub页面
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: packages/docs/.vitepress/dist